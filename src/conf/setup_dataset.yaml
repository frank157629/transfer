wandb:
  api_key: "63fa0006025de09419d138e6bf2e67637584944a"
  project: "Project_PINN_for_GFM"

time : 1 # Total time
num_of_points : 1000 # Number of points
modelling_method : True # Modelling approach

model:
  model_flag : "GFM"
  machine_num : 1
  init_condition_bounds : 1
  sampling: "Linear" # Choose between "Lhs" or "Linear" or "Random"
  torch : False

dirs:
  params_dir :  ${hydra:runtime.cwd}/src/conf/params
  init_conditions_dir : ${hydra:runtime.cwd}/src/conf/initial_conditions
  dataset_dir : ${hydra:runtime.cwd}/data